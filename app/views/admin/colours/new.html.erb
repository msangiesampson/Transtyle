<h2>New Colour</h2>
<hr/>
<%= simple_form_for [:admin, @colour] do  |f|%>
  <%= f.input :name %>
  <%= f.input :colour, hint: "color code, don't use #" %>
  <%= f.submit "Create", class: 'button' %>
<% end %>
